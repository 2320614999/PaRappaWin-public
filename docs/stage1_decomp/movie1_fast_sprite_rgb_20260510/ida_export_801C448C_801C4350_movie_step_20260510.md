# IDA current-IDB export: 801C448C / 801C4350 movie step

## `801C448C` `sub_801C448C`

```c
BOOL __fastcall sub_801C448C(int a1, int a2)
{
  sub_80035560(2);
  sub_8001A3C8();
  sub_8001A280();
  return sub_801C4350(a1, a2);
}
```

## `801C4350` `sub_801C4350`

```c
BOOL __fastcall sub_801C4350(int a1, int a2)
{
  int v4;
  BOOL result;

  v4 = sub_8001A7A4(*(_DWORD *)(a1 + 40));
  result = 1;
  if ( v4 >= 0 )
  {
    if ( v4 - dword_801C954C <= 0 )
    {
      if ( dword_801C954C - v4 >= 301 )
        return 1;
    }
    else
    {
      result = 1;
      if ( v4 - dword_801C954C >= 301 )
        return result;
    }
    dword_801C954C = v4;
    *(_WORD *)(a2 + 4) = v4 / 5 / 1800;
    *(_BYTE *)(a2 + 7) = v4 / 5 % 30;
    *(_BYTE *)(a2 + 6) = v4 / 5 % 1800 / 30;
    return sub_8001A7F8(a1) != 1;
  }
  return result;
}
```

## Translation notes

- `801C448C` is a thin wrapper; it does not justify a Win-side `strPlayed`
  inference.
- `801C4350` owns `dword_801C954C` and the ctx timecode writes at
  `ctx+4`, `ctx+6`, and `ctx+7`.
- The only still external facts are `8001A7A4(*(a1+40))` and
  `8001A7F8(a1)`.

## `8001A7A4` `sub_8001A7A4`

```c
int __fastcall sub_8001A7A4(int a1)
{
  dword_80049404 = sub_80036A78((unsigned __int8 *)&byte_800493F4) + 150;
  return sub_80036A78((unsigned __int8 *)&byte_800493F4) - a1;
}
```

## `8001A7F8` `sub_8001A7F8`

```c
BOOL __fastcall sub_8001A7F8(int a1)
{
  return *(_DWORD *)(a1 + 44) + *(_DWORD *)(a1 + 8) <= dword_80049404;
}
```

## Additional translation notes

- `8001A7A4` depends on the shared `80036A78(byte_800493F4)` clock and
  the segment field passed from `*(a1+40)`.
- `8001A7F8` compares segment fields `a1+44` and `a1+8` against
  `dword_80049404`; the first Hex-Rays listing simplified this incorrectly to
  `<= 0`, so the asm/static segment-table check is the translation authority.

## `80036A78` `sub_80036A78`

```c
int __fastcall sub_80036A78(unsigned __int8 *a1)
{
  return 75 * (60 * (10 * (*a1 >> 4) + (*a1 & 0xF)) +
               10 * (a1[1] >> 4) + (a1[1] & 0xF))
       + 10 * (a1[2] >> 4)
       + (a1[2] & 0xF)
       - 150;
}
```

## Additional `80036A78` note

- `80036A78` is a pure BCD MSF to LBA conversion:
  `((mm * 60 + ss) * 75 + ff) - 150`.
