# MenuHelp 80026314 context-builder export (2026-05-11)

Scope: close the caller/context side around `80026314` without treating replay
or Win HUD state as authority. `80026314` itself is already a callee-side text
producer; this file tracks the missing upstream evidence.

## Exported artifacts

- S1 COMOD1 formal export:
  `menuhelp_80026314_context_20260511/S1_COMOD1_manifest.json`.
- Dumped functions:
  `S1_COMOD1/decompile_801ca3bc.txt`,
  `S1_COMOD1/decompile_801ca0e0.txt`,
  `S1_COMOD1/decompile_801ca1ec.txt`,
  `S1_COMOD1/decompile_801ca25c.txt`,
  `S1_COMOD1/decompile_801ca264.txt`,
  `S1_COMOD1/decompile_801c8660.txt`.
- Missing-function entries in this IDB batch:
  `801C5B14`, `801C57C8`, `801C57D0`, `801C57D8`, `801C5B0C`,
  `801C4FC8`.
- SCUS standalone artifacts already exist for
  `docs/stage1_decomp/80014614_sub_80014614.c` and
  `docs/stage1_decomp/80024FD0_sub_80024FD0.c`.
- SCUS standalone artifacts now also exist for
  `docs/stage1_decomp/80024BC0_sub_80024BC0.c` and
  `docs/stage1_decomp/80026FC4_sub_80026FC4.c`.
- Table sweep artifact:
  `docs/stage1_decomp/ida_export_menuhelp_80026314_table_sweep_20260512.json`.

## Static facts

- `801CA3BC` initializes the Stage1 global table and writes
  `80094430=801CA0E0`, `80094434=801CA1EC`, `80094438=801CA25C`,
  `8009443C=801CA264`, and `80094440=801C8660`.
- `801CA0E0` is `PrStageRunner_HudOverlay_Configure`: it sets the runner
  flag bit `0x10`, pulls Stage1 overlay config from `801D2DD0`, writes
  `ctx+0xDC`, `ctx+0xE8`, `ctx+0x11C`, `ctx+0x120`, and may seed
  `g_PrHudOverlaySlots[0]` from `801CF924 + 12 * id`.
- `801C8660` is `PrEventTable_Build`: it walks `801D2E2C`, writes records
  derived from `24 * row[0]` and `80024BC0(row[1])`, then stores
  `800901BC=dword_801D2F00`.
- Current SCUS artifact: `80014614` consumes source cells and calls
  `80026FC4(sourceCell)`; if `sourceCell+8` is nonzero it dispatches through
  `dword_80094430(a1)`. This proves a callback slot route, but not a
  `80026314` caller.
- Current SCUS artifact: `80024FD0` builds the stage runner context and
  stage/event table pointers.
- Current SCUS artifact: `80024BC0` maps stage ids through `dword_80054168`.
- Current SCUS artifact: `80026FC4` initializes the six-byte payload copied to
  `byte_800943AE`.

## Boundary

- No exported S1 COMOD1 or SCUS path above writes `0x80026314` into
  `80094430..80094440`, and no path proves a direct or indirect call to
  `80026314`.
- `80094430`/`sourceCell+8` is a callback mechanism, not MenuHelp proof by
  itself.
- The 2026-05-12 table sweep found no direct xref, operand hit, raw dword hit,
  or `jal 80026314` encoding for `80026314`. It only found slot references to
  `80094430` from `80014614` and `801C5B14`, to `80094438` from `80024FD0`
  and `801C5B14`, and the remaining `80094434/3C/40` writes from `801C5B14`.
- Next useful export is not `80026314` body again; it is a full
  function-pointer/table sweep that finds where a `80026314` context is
  constructed, if such a path exists.
