# scene entry memcard/hi-score closure: 800191E4 / 80019284

Source: IDA MCP direct decompile from `SCUS_941.83`, 2026-05-12.

## 800191E4 sub_800191E4

```c
int __fastcall sub_800191E4(int a1, int a2)
{
  int v3; // $v0
  int v5; // $gp
  int v6; // $gp
  int v7; // $a0

  v3 = sub_80026784();
  sub_80025C64(v3, &byte_8007CC50, 36);
  *(_DWORD *)(v5 + 716) = 0;
  *(_DWORD *)(v5 + 732) = a2;
  sub_80017524();
  sub_80018FB0(a1, sub_80018E10, sub_80019D7C, 20, 3);
  sub_80017574();
  v7 = -1;
  if ( *(_DWORD *)(v6 + 716) == 1 )
    return *(_DWORD *)(a1 + 44);
  return v7;
}
```

## 80019284 sub_80019284

```c
int *__fastcall sub_80019284(int a1)
{
  int v1; // $s6
  int v2; // $s5
  int v3; // $fp
  int v4; // $s2
  int v5; // $s4
  int *v6; // $s3
  int v7; // $s0
  const char *v8; // $s1
  int v9; // $a2
  _BYTE v11[32]; // [sp+10h] [-38h] BYREF
  int v12; // [sp+30h] [-18h]

  v1 = 0;
  v2 = 0;
  v12 = a1;
  v3 = 4876;
  do
  {
    v4 = 0;
    v5 = 16 * v1 + 20;
    v6 = &dword_80049278[4 * v1];
    v7 = v2 << 6;
    v8 = (const char *)(v12 + v3);
    do
    {
      v9 = *(_DWORD *)(v12 + v7 + 4888);
      if ( v9 > 0 )
      {
        sub_8004800C(v11, "%4d %-3.3s", v9, v8);
        sub_80017FC4((char *)dword_80049278 + v5, v11);
      }
      else
      {
        *((_BYTE *)v6 + 20) = 0;
      }
      v8 += 16;
      v7 += 16;
      ++v4;
      v5 += 16;
      v6 += 4;
      ++v1;
    }
    while ( v4 < 3 );
    ++v2;
    v3 += 64;
  }
  while ( v2 < 6 );
  return dword_80049278;
}
```

## Translation notes

- `800191E4(a1, mode)` writes `gp+732 = mode`, resets `gp+716 = 0`, runs
  `80018FB0(a1, 80018E10, 80019D7C, 20, 3)`, then returns
  `*(s32*)(a1+44)` only if `gp+716 == 1`; otherwise it returns `-1`.
- `80019414(a1)` ignores the return value of `800191E4(a1, 3)` and gates only
  on `gp+720 == 1`.
- `80019284(a1)` builds 18 records in `dword_80049278`, laid out as 6 rows x
  3 columns. For each cell it reads score at `a1 + row*64 + col*16 + 4888`
  and name at `a1 + 4876 + row*64 + col*16`; positive scores are formatted
  as `"%4d %-3.3s"` and copied to `dword_80049278 + 20 + cell*16`.
