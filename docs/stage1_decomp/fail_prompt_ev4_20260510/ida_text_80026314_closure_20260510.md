# TEXT 80026314 export closure

Scope: static-only direct-port note. No build, no test, no code edits.

Evidence:

- Current IDA MCP pseudocode for `sub_80026314(a1)` from the active export context.
- `ida_export_record_producer_80043A14_800436F0_20260510.txt` disasm facts for `80026314`.
- Explicit field model supplied for the `a1` menu/help context.

## Conclusion

`80026314` is a higher-level TEXT producer for a menu/help prompt. It builds a text block by appending strings through `80043A14`, then flushes through `800436F0(-1)` at `80026478`.

Do not connect this to Win HUD yet. The blocker is still upstream ownership: `XrefsTo 80026314` and the caller/data source for the `a1` context have not been exported.

## Pseudocode Shape

Current Hex-Rays shape, normalized only for readability:

```c
int __fastcall sub_80026314(int a1)
{
  group = *(int **)a1;
  group_index = 0;
  sub_80043A14("\n\n\n\n");

  if (*(int *)(a1 + 8) > 0) {
    selected_item_ptr = group + 8; // group + 0x20 as dword index
    do {
      item_index = 0;
      sub_80043A14(selected_group_marker);
      sub_80043A14("%s:", group_title);

      if (*group > 0) {
        item = group;
        do {
          prefix = "~c000 ";
          if (item_index == *selected_item_ptr)
            prefix = "~c888*";

          strcpy(stack, prefix);
          strcat(stack, item[2]);       // item + 0x08 label ptr
          if (strlen(item[2]) < 6)
            append spaces until label length is 6;

          sub_80043A14(stack);
          ++item_index;
          ++item;
        } while (item_index < *group);
      }

      ++group_index;
      sub_80043A14("\n");
      selected_item_ptr += 9;
      group += 9;
    } while (group_index < *(int *)(a1 + 8));
  }

  sub_80043A14("\n\n~c222      O: OK   X: CANCEL~c888\n");
  return sub_800436F0(-1);
}
```

This is only the exported function body. No caller is established by this export.

## Disasm Facts

Confirmed calls inside `80026314`:

| PC | Callee | Fact |
|---|---|---|
| `80026348` | `80043A14` | Appends `"\n\n\n\n"`. |
| `80026380` | `80043A14` | Appends group marker: normal `"~c000    "` or selected `"~c888 >>>"`. |
| `80026394` | `80043A14` | Appends `"%s:"` with group title pointer loaded from group `+0x04`. |
| `80026424` | `80043A14` | Appends stack-built item row string. |
| `80026448` | `80043A14` | Appends `"\n"` after each group. |
| `80026470` | `80043A14` | Appends footer `"\n\n~c222      O: OK   X: CANCEL~c888\n"`. |
| `80026478` | `800436F0` | Flushes with `a0 = -1`; not a producer call. |

Item row construction before `80026424` is explicit in disasm: choose prefix, `strcpy` to stack buffer, append label with `strcat`, measure label with `strlen`, append spaces until label width is at least 6, then pass the completed stack buffer to `80043A14`.

## Field Model

Context `a1`:

| Offset | Meaning |
|---|---|
| `a1+0` | group bank pointer |
| `a1+4` | selected group index |
| `a1+8` | group count |

Group row:

| Offset | Meaning |
|---|---|
| `group+0x00` | item count |
| `group+0x04` | title pointer |
| `group+0x20` | selected item index |

Traversal:

- Group stride is `0x24`.
- Item row is dword-stride from the current group base.
- Item label pointer is `item+0x08`.

## Closure State

Closed:

- `80026314` local producer order is known.
- `80043A14` is the append boundary used by this function.
- `800436F0(-1)` is the flush boundary after the producer finishes.
- Stack-buffer item row construction is known and must be preserved for direct-port.
- The `a1`/group/item field model is explicit enough to translate this body once caller ownership is known.

Open:

- `XrefsTo 80026314` has not been exported.
- The caller and lifetime of the `a1` context are unknown.
- The group bank source behind `a1+0` is unknown.
- HUD connection is blocked until caller/data source proves when this TEXT producer runs and which object it receives.

## Next Export List

1. Export `XrefsTo 80026314` with caller function boundaries and disasm around each callsite.
2. Export pseudocode/disasm for every confirmed caller of `80026314`.
3. For each caller, trace the value passed in `a0` into `80026314` and identify the concrete `a1` context object.
4. Export writes/builders for context fields `a1+0`, `a1+4`, and `a1+8`.
5. Export writes/builders for group/item fields `group+0x00`, `group+0x04`, `group+0x20`, and `item+0x08`.
6. After caller/data source closure, decide the Win cutover boundary for this TEXT producer; until then, do not wire it to HUD.
