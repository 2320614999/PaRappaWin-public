# TEXT 80026314 XrefsTo pre-export

Scope: static-only IDA evidence for the new-module "export -> graph" prework.
No build, no test, no code edits, no HUD hookup, no Win shell patch proposal.

Target: `TEXT:80026314`, `sub_80026314`, bounds `80026314-800264AC`.

Active IDB checked before trusting xrefs:

```text
path   E:\game\PS模拟器\parappa the rapper\SCUS_941.83
module SCUS_941.83
md5    d425871c4cec676c5d0f783a33b367a6
```

Relevant IDA segments:

```text
80000000-80010000 RAM  DATA
80010000-8006F000 TEXT CODE
8006F000-80200000 RAM  DATA
```

## Conclusion

`XrefsTo(80026314)` is empty in the active SCUS IDB.

Additional static sweeps also found no caller:

| Sweep | Result |
|---|---:|
| `idautils.XrefsTo(0x80026314)` | `0` |
| instruction operand value equal to `0x80026314` | `0` |
| raw little-endian dword `14 63 02 80` | `0` |
| raw `jal 80026314` encoding `C5 98 00 0C` | `0` |
| disassembly text mention of `80026314` outside target | `0` |

Therefore the requested caller boundary/callsite export has no positive entries
in this database. The current static evidence does not prove who owns or builds
the context object passed to `80026314`; it only proves the callee-side context
layout consumed by `80026314`.

Do not connect this to HUD from this evidence. The next graph step needs either
the real indirect dispatch source, an overlay/other IDB that contains the caller,
or a runtime entry trace only to locate the static caller.

## XrefsTo 80026314

IDA export:

```text
TARGET 80026314 name=sub_80026314 func=80026314-800264AC sub_80026314

XrefsTo target:
  <none>
```

No caller function boundaries can be listed because there are no `XrefsTo`
entries and no raw direct-call hits.

| Caller | Boundary | Callsite | Status |
|---|---|---|---|
| `<none>` | `<none>` | `<none>` | No direct static xref in active SCUS IDB. |

## Negative Callsite Evidence

Expected direct MIPS `jal 80026314` encoding:

```text
JAL_WORD 0C0098C5
little-endian bytes C5 98 00 0C
```

Search result:

```text
Instruction operand hits for target value: 0
Byte search target_dword_le 14 63 02 80: 0 hit(s)
Byte search jal_encoding_le C5 98 00 0C: 0 hit(s)
Textual disasm mentions 80026314: 0
```

This closes the direct-xref/callsite sweep for the current IDB. It does not
close indirect invocation through tables, script dispatch, overlays, or runtime
state that is not represented as a `0x80026314` immediate in this database.

## Target Pseudocode Shape

Hex-Rays names the function parameter `a1`, but the physical MIPS incoming
register is `$a0`. The entry instruction copies `$a0` into `$s6`; `$s6` is then
used as the context pointer.

```c
int __fastcall sub_80026314(int a1)
{
  int *v2;        // $s3
  int v3;         // $s5
  _DWORD *v4;     // $s4
  int v5;         // $s1
  int *v6;        // $s2
  const char *v7; // $a1
  int v8;         // $v0
  int i;          // $s0
  _BYTE v11[2];   // stack buffer

  v2 = *(int **)a1;
  v3 = 0;
  sub_80043A14();
  if (*(int *)(a1 + 8) > 0) {
    v4 = v2 + 8;
    do {
      v5 = 0;
      sub_80043A14();
      sub_80043A14();
      if (*v2 > 0) {
        v6 = v2;
        do {
          v7 = "~c000 ";
          if (v5 == *v4)
            v7 = "~c888*";
          strcpy(v11, v7);
          strcat(v11, v6[2]);
          strlen();
          if (v8 < 6) {
            for (i = 6 - v8; i > 0; --i)
              strcat(v11, " ");
          }
          sub_80043A14();
          ++v5;
          ++v6;
        } while (v5 < *v2);
      }
      ++v3;
      sub_80043A14();
      v4 += 9;
      v2 += 9;
    } while (v3 < *(_DWORD *)(a1 + 8));
  }
  sub_80043A14();
  return sub_800436F0();
}
```

## 80026314 Entry And Context Data Flow

Key disassembly:

```asm
80026314: addiu   $sp, -0x80
80026318: sw      $s6, 0x60+var_s18($sp)
8002631C: move    $s6, $a0
80026320: li      $a0, asc_8006EBF0      # "\n\n\n\n"
80026344: lw      $s3, 0($s6)
80026348: jal     sub_80043A14
80026350: lw      $v0, 8($s6)
80026360: addiu   $s4, $s3, 0x20
80026364: lw      $v0, 4($s6)
80026388: lw      $a1, -0x1C($s4)
8002638C: li      $a0, aS_2              # "%s:"
80026394: jal     sub_80043A14
800263B0: lw      $v0, 0($s4)
800263D4: lw      $a1, 8($s2)
800263E0: lw      $a0, 8($s2)
80026424: jal     sub_80043A14
80026428: addiu   $a0, $sp, 0x60+var_50
80026450: addiu   $s4, 0x24
80026464: addiu   $s3, 0x24
```

Initial field judgment from callee-side reads:

| Field | Evidence | Preliminary meaning |
|---|---|---|
| incoming `$a0` / Hex-Rays `a1` | `8002631C move $s6, $a0` | menu/help context pointer |
| `ctx+0x00` | `80026344 lw $s3, 0($s6)` | group bank/base pointer |
| `ctx+0x04` | `80026364 lw $v0, 4($s6)` | selected group index |
| `ctx+0x08` | `80026350`, `80026454 lw $v0, 8($s6)` | group count |
| `group+0x00` | `8002639C lw $v0, 0($s3)` | item count |
| `group+0x04` | `80026388 lw $a1, -0x1C($s4)`, with `$s4=group+0x20` | group title string pointer |
| `group+0x20` | `800263B0 lw $v0, 0($s4)` | selected item index |
| `item+0x08` | `800263D4`, `800263E0 lw ?, 8($s2)` | item label string pointer |
| group stride | `80026450 addiu $s4, 0x24`; `80026464 addiu $s3, 0x24` | `0x24` bytes per group |
| item stride | `8002643C addiu $s2, 4` | dword step per item slot |

Physical `$a1` inside this function is not the original context pointer. It is
used as a second argument to callees after the function has already saved the
incoming context in `$s6`:

| PC | `$a0` | `$a1` | Meaning |
|---|---|---|---|
| `80026394` | `"%s:"` | `*(group+0x04)` | `80043A14` vararg append for group title. |
| `800263CC` | stack buffer | item marker literal | `strcpy(stack, marker)`. |
| `800263D8` | stack buffer | `*(item+0x08)` | `strcat(stack, item label)`. |
| `80026410` | stack buffer | `" "` | pad item label to width 6. |

## XrefsFrom 80026314

These are callee/data references from the function body. They are included only
to anchor the local producer sequence; they are not callers of `80026314`.

| From | To | Meaning |
|---|---|---|
| `80026320` | `8006EBF0` | initial `"\n\n\n\n"` literal |
| `80026348` | `80043A14` | text append |
| `80026368` | `800112E0` | normal group marker `"~c000    "` |
| `80026378` | `800112D4` | selected group marker `"~c888 >>>"` |
| `80026380` | `80043A14` | text append |
| `8002638C` | `8006EBF8` | `"%s:"` format |
| `80026394` | `80043A14` | vararg text append |
| `800263B4` | `8006EC04` | normal item marker `"~c000 "` |
| `800263C4` | `8006EBFC` | selected item marker `"~c888*"` |
| `800263CC` | `80047F9C` | `strcpy` |
| `800263D8` | `80047F6C` | `strcat` |
| `800263E4` | `80047FAC` | `strlen` |
| `80026408` | `8006EC0C` | single-space literal |
| `80026410` | `80047F6C` | `strcat` padding |
| `80026424` | `80043A14` | append completed stack item row |
| `80026440` | `8006EC10` | newline literal |
| `80026448` | `80043A14` | text append |
| `80026468` | `800112EC` | footer literal |
| `80026470` | `80043A14` | text append |
| `80026478` | `800436F0` | flush with `a0=-1` |

## Graph Boundary For Next Step

Current known node:

```text
unknown caller/dispatcher
  -> sub_80026314(ctx)          ; no direct static xref in current SCUS IDB
       -> 80043A14(...)         ; append text records
       -> strcpy/strcat/strlen  ; local item-row stack producer
       -> 800436F0(-1)          ; flush
```

Do not draw a concrete upstream edge yet. The only defensible graph edge from
this export is inside the callee: `ctx` flows through `$s6` into group/item
fields and then into `80043A14` text appends.

Open evidence:

- Identify whether `80026314` is invoked indirectly by a table/script
  dispatcher, from another overlay IDB, or from runtime-only state.
- Once a positive caller is located, export that caller boundary and the exact
  callsite window.
- Trace the caller-side `$a0` producer into the context object before treating
  `ctx+0`, `ctx+4`, and `ctx+8` as Win-owned inputs.
