# StageClear/MenuHelp Live Gap `800916F6/800167A8/80026314` - 2026-05-13

Sources:

- Current SCUS IDA export:
  `docs/stage1_decomp/ida_export_stageclear_menuhelp_live_gap_800916f6_800167a8_80026314_20260513.txt`
- Prior dynamic/xref note:
  `docs/stage1_decomp/menuhelp_stageclear_dynamic_xrefs_20260513.md`
- Save/status follow-up:
  `docs/stage1_decomp/save_status_8001635c_callchain_20260513.md`

This is evidence only. It does not authorize HUD/TEXT fallback, Win state,
default `word_800916F6`, replay-sampled payload, or `800167A8` as a substitute
caller.

## Current Static Facts

- `80026314` has no current SCUS xrefs.
- `800167A8` has no current SCUS xrefs.
- `word_800916F6` has only one current SCUS xref:
  `80026B94:80026D70 lh word_800916F6`.
- `byte_80092F1D` xrefs remain `800166AC`, `800167A8`, and `80026B94`.
- Raw locator scans in the current SCUS database found no direct dword hit for
  `80026314`, `800167A8`, or `800916F6`.
- Direct `jal` encoding scans found no hit for `80026314` or `800167A8`.

## StageClear Branch

`80026B94` gates StageClear text on `a1 == 2` and nonzero `word_800916F6`:

```text
80026D64: li      $v0, 2
80026D68: bne     $s3, $v0, loc_80026DC0
80026D70: lh      $v0, word_800916F6
80026D7C: beqz    $v0, loc_80026DC0
80026D84: li      $a0, aC000stageclear
80026D8C: jal     sub_80043A14
80026D94: li      $a0, dword_8006EC14
80026D9C: li      $at, byte_80092F1D
80026DA8: lbu     $a1, 0($at)
80026DAC: jal     sub_80043A14
```

This proves the read/consumer side. It still does not identify a writer for
`word_800916F6`.

## `800167A8` Boundary

`800167A8` is still only a leaf status writer for `byte_80092F1D + idx`:

```text
800167C4: jal     sub_8001615C
800167D4: li      $v1, byte_80092F1D
800167E8: bne     $v1, $a0, loc_800167FC
800167F0: li      $v1, 2
800167FC: li      $v1, 1
80016800: sb      $v1, 0($v0)
```

No caller is closed in the current SCUS IDA database or the available
recordings.

## `80026314` Boundary

`80026314` remains a MenuHelp formatter/draw leaf. The current export still has
no direct xref, raw dword hit, or `jal 80026314` hit in SCUS. Its `a0` context
builder and table source remain the open question.

## Remaining Evidence Window

The next useful evidence remains dynamic:

- `exec 80026314` with `ra/sp/a0-a3` and `a0..a0+0x80`
- `exec 800167A8` with `ra/sp/a0-a3` and `80092F10..80092F60`
- `write 800916F6` with writer pc and `800916E0..80091700`
- `write 80092F1D..80092F22` only to distinguish `8001635C` max-promote from
  `800167A8` leaf writes

The current save/load recording closes `80092F1D` only to `8001635C`; it does
not close `80026314`, `800167A8`, or `800916F6`.
