# IDA export: movie segment CD lookup / stream clock producers - 2026-05-12

Authority: IDA MCP current IDB `E:\game\PS模拟器\parappa the rapper\SCUS_941.83`.

## `8001A2B0`

```c
int __fastcall sub_8001A2B0(int a1, int a2)
{
  int v2;
  int i;

  v2 = a1;
  if ( !a2 )
    return 0;
  for ( i = 0; i < 4; ++i )
  {
    if ( sub_800381F8(a1, a2) )
      return v2;
    a1 = v2;
  }
  return 0;
}
```

## `800381F8`

Relevant success-path facts from the full IDA pseudocode:

- requires `sub_800384FC() != 0`;
- rejects paths whose first byte is not `'\\'`;
- walks up to eight path components using `sub_800387D8`;
- requires the final filename component to match a directory entry via `sub_800384D8`;
- on match, copies six dwords from the directory entry to `a1[0..5]`;
- returns the matched directory entry pointer on success, otherwise `0`.

This confirms `CdlFILE.pos/size/name[16]` are one 24-byte payload from
`800381F8`, not separate Win filesystem facts.

## `8001A210`

```c
int __fastcall sub_8001A210(char a1, _BYTE *a2)
{
  int result;

  result = 2;
  if ( a1 == 2 )
  {
    byte_800493F4 = a2[2];
    byte_800493F5 = a2[3];
    result = (unsigned __int8)a2[4];
    byte_800493F6 = a2[4];
  }
  return result;
}
```

## `8001A3C8`

```c
int sub_8001A3C8()
{
  int v0;
  int result;
  bool v2;
  _BYTE v3[16];

  v0 = sub_800364D0(1, (int)v3);
  if ( v0 == 5 && (v3[0] & 0x10) != 0 )
  {
    dword_80049428 = 1;
    return 0;
  }
  else
  {
    result = 0;
    if ( v0 == 2 )
    {
      v2 = sub_800363A4() == 13;
      result = 1;
      if ( v2 )
      {
        return 0;
      }
      else
      {
        dword_80049428 = 0;
        byte_800493F4 = v3[0];
        byte_800493F5 = v3[1];
        byte_800493F6 = v3[2];
      }
    }
  }
  return result;
}
```

## Implementation checkpoint

- `PrStage1LoaderCdHal::LookupFeedback800381F8` already carries `pos`, `size`
  and `name[16]`.
- `PrStage1LoaderCdHal::BuildIso9660LookupFeedback800381F8(...)` fills all
  three pieces from the ISO9660 directory record.
- `PrStage1MovieSegmentDirect::BuildMovieSegmentRowFeedbackFromCdLookup8001A2B0(...)`
  forwards all three pieces into `MovieSegmentRowInitFeedback8001A324`.
- `PrMovieSegmentDirect::PsxCall8001A210_StreamClockCallback(...)` and
  `PsxCall8001A3C8_StreamClockPoll(...)` already represent the two
  `byte_800493F4` writer functions.
- Remaining runtime gap is lower CD sync/callback feedback into these direct
  functions; do not source clock bytes from STR duration, movie frame, host
  playback state, or file metadata.
