# `byte_800493F4` stream clock producer - 2026-05-10

## IDA authority

`sub_8001A210(a1, a2)`:

```c
int __fastcall sub_8001A210(char a1, _BYTE *a2) {
  int result = 2;
  if (a1 == 2) {
    byte_800493F4 = a2[2];
    byte_800493F5 = a2[3];
    result = (unsigned __int8)a2[4];
    byte_800493F6 = a2[4];
  }
  return result;
}
```

`sub_8001A258()` registers that callback:

```c
int sub_8001A258() {
  return sub_80036510((int)sub_8001A210);
}
```

`sub_80036510(callback)`:

```c
int __fastcall sub_80036510(int a1) {
  dword_800570F8 = a1;
  return 0;
}
```

`sub_8001A3C8()` polls CD sync bytes and may also write the clock:

```c
int sub_8001A3C8() {
  _BYTE v3[16];
  int v0 = sub_800364D0(1, (int)v3);
  if (v0 == 5 && (v3[0] & 0x10) != 0) {
    dword_80049428 = 1;
    return 0;
  }
  if (v0 == 2) {
    if (sub_800363A4() == 13) {
      return 0;
    }
    dword_80049428 = 0;
    byte_800493F4 = v3[0];
    byte_800493F5 = v3[1];
    byte_800493F6 = v3[2];
    return 1;
  }
  return 0;
}
```

`sub_8001A724(a1)` resets watchdog state:

```c
int __fastcall sub_8001A724(int a1) {
  dword_80049404 = 150;
  dword_80049408 = -a1;
  return 0;
}
```

`sub_8001A7A4(timeBase)` remains the consumer:

```c
int __fastcall sub_8001A7A4(int a1) {
  dword_80049404 = sub_80036A78((unsigned __int8 *)&byte_800493F4) + 150;
  return sub_80036A78((unsigned __int8 *)&byte_800493F4) - a1;
}
```

## Implementation state

- `PrMovieSegmentDirect` now has direct producer carriers for `8001A210`,
  `8001A3C8`, and `8001A724`.
- `PrMovieSegmentDirect` also carries the lower CD-sync boundary:
  - `800363A4` reads `byte_80057119`;
  - `800364D0` is a wrapper around `80037070`;
  - `80037070` consumes explicit CD sync lower feedback, returns the sync
    result, and copies the returned result bytes to the caller buffer only when
    the lower feedback supplies them.
- `PrStage1XaCdDirectState` now stores explicit `byte_800493F4[3]`,
  producer function address, `dword_80049404/408`, and `dword_80049428`.
- `PrStage1XaCdDirectState` now also stores explicit `byte_80057119` and the
  latest `800364D0/80037070` CD sync result; this can be fed into
  `8001A3C8` without deriving clock from host playback.
- `PrStage1XaCdDirectProbeStreamClockProducer800493F4(...)` only emits a
  clock carrier when the state was written by one of the translated producer
  functions.
- Loader batch feedback now remains known for explicit stream-clock carrier or
  explicit missing-producer gap, even when no movie-segment lookup probe was
  matched in the same batch.

## Remaining gap

No Win-side fallback is accepted. The next direct-port step is to feed
`PrStage1XaCdDirectApplySub8001A210ClockCallback(...)` or
`PrStage1XaCdDirectApplySub8001A3C8ClockPoll(...)` from a PSX-equivalent CD
callback/sync feedback source. Do not derive these bytes from STR duration,
movie frame, packet frame index, lookup start MSF, or host playback state.
